import * as React from 'react'
import { HStack, VStack, Image} from 'native-base'

const TicketCard = () => {
  return (
    <HStack>
      <Image>
      <VStack>
        
      </VStack>
    </HStack>
  )
}

export default TicketCard